import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'capacitor.screen.orientation.bug.reproduction',
  appName: 'capacitor-screen-orientation-bug-reproduction',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
